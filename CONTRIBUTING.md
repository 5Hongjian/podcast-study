# 迭代规则

每轮从最新 `main` 新建 `codex/<change-name>` 分支。一轮解决一个清楚的问题，提交中说明触发条件、改变后的行为和验证结果。下一轮继续同一仓库，不另建一个无法对照的新副本。

1. 在 Issue 或本地说明中记录具体问题和验收条件。公开材料使用虚构或已许可的最小例子，不上传真实任务日志、私人 Docs、音频、字幕或账户信息。
2. 在仓库内修改通用技能及必要资源。用户自己的格式或偏好仅改本地覆盖；不为单次个例增加普遍限制。
3. 运行 `python3 scripts/validate.py` 和 `python3 -m unittest discover -s tests -v`；按改动选择行为场景。变更安装器必须验证原设置、旧记录和备份恢复。
4. 审阅 `git diff` 与将提交的文件清单。创建 PR，描述最终行为及未验证的范围；不贴私人运行输出。
5. 检查通过后合并 `main`。发布时同步 `VERSION`、SKILL.md 的 `metadata.version`、空白模板 `skill_version` 及 `CHANGELOG.md`，再为该提交打 `vX.Y.Z` 标签。

版本约定：修错与表述澄清升补丁版本；新增兼容能力升次版本；破坏安装路径、配置或记录兼容性的改动升主版本并提供迁移说明。工作流版本描述流程，`schema_version` 只描述记录结构，不能把三者混为一谈。

变更默认结构不会覆盖用户的本地文件；用户未覆盖的默认值会随技能更新。旧任务恢复按已记录的阶段和证据继续，不为了让历史记录看起来“新”而倒填检查结果。

需要回退时，检出已验证标签并重新安装。不要强制推送覆盖共享历史；通过新的修复或回退提交保留审计记录。安装器备份和真实任务状态只存本地。
